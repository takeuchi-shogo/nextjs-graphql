# query Accounts {
# 	account {
# 		phone_number
# 		email
# 		password
# 	}
# }

query users {
	users {
		id
		display_name
	}
}

query user($id: ID!) {
	user(id: $id) {
		id
		uuid
		display_name
	}
}

# query get_screen_name {
# 	screen_name
# }

query GetVerifyEmailByPinCode($code: String!) {
	verify_email(code: $code) {
		email
	}
}
